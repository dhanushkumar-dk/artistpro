// SellingFast.jsx
import React from "react";

const SellingFast = ({ filter }) => {
  return (
    <div className="selling-fast">
      <h2>Selling Fast - Filter: {filter}</h2>
      {/* Display events based on filter */}
    </div>
  );
};

export default SellingFast;
